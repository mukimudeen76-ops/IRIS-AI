const ConnectMobileSetup = ({
  mobile,
  setupType,
  useWiFi,
  appPackageName,
  appVersion,
  appPath,
  adbPath,
}: {
  mobile: string;
  setupType: string;
  useWiFi: boolean;
  appPackageName: string;
  appVersion: string;
  appPath: string;
  adbPath: string;
}) => {

  
};